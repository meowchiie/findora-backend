const path = require('path');

module.exports = {
  // Tentukan lokasi baru untuk folder config database
  'config': path.resolve('config', 'config.js'),
  
  // Ubah lokasi folder models sesuai keinginan Anda
  'models-path': path.resolve('src', 'models'),
  
  // Ubah lokasi folder migrations sesuai keinginan Anda
  'migrations-path': path.resolve('database', 'migrations'),
  
  // Ubah lokasi folder seeders (jika digunakan)
  'seeders-path': path.resolve('database', 'seeders')
};